[project]
name = "mini-runtime"
version = "0.1.0"
description = "A lightweight LLM inference engine with continuous batching, paged KV cache, and prefix cache"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "transformers>=4.40",
    "safetensors>=0.4",
    "huggingface-hub>=0.20",
]
